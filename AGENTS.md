# Codex Repository Rules

1. 不允许直接修改 `main`；每个任务使用独立 `exp/<task-id>-<name>` 分支。
2. 一次只执行一个 `tasks/Txxx_*.md`。
3. 未在 `DECISIONS.md` 中冻结的论文歧义不得自行决定。
4. 遇到未决歧义时，停止相关实现并提交 `BLOCKER`，不要猜测。
5. 先实现可读、可审计的数学参考版本，再做性能优化。
6. 所有随机过程必须接受显式 seed，测试不得依赖系统随机状态。
7. 所有数组/向量必须检查 shape、dtype、模数域和编码长度。
8. 哈希必须使用域分离标签；禁止直接拼接含糊的可变长字段。
9. 不得用 mock、硬编码会话密钥或跳过验证来让测试通过。
10. 每个 benchmark 必须输出原始 CSV，不得只输出截图或平均值。
11. 图表必须由 CSV 自动生成，禁止手填数据。
12. 结果必须记录 Git commit、profile、seed、运行环境和依赖版本。
13. 不得为了匹配论文运行时间删除论文算法要求的计算。
14. 不得把 `paper_literal` 与 `audited` 的代码路径或结果混在一起。
15. 不得声称代码验证了安全证明；只能陈述已验证的可执行性质。
16. 每次提交前运行：格式检查、静态类型检查、单元测试、协议测试。
17. 默认使用 Python 3.11+；参考实现优先 NumPy，密码散列优先 hashlib/SHAKE。
18. 对外部库的调用必须封装为 backend，并保留纯 Python/NumPy 参考接口。
19. PDF 不得提交到公开仓库；只提交论文元数据、页码索引和用户自行下载说明。
20. 每轮必须按 `templates/RESULT_TEMPLATE.md` 生成结果文件。
